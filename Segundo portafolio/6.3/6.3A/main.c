#include <stdio.h>
#include <stdlib.h>

int main()
{
    float num=2,num1=0, resp;
    printf("Este programa suma una serie.\n");
    printf("Digite el valor del n%cmero:\n",163);
    scanf("%f",&num);
    while(num>=50)
        {
        printf("Error");
        }

    for(num=2; num<=50 ; num++)

    num1= num1 + num;
    printf("El resultado es: %.2f", num1);

    return 0;
}
